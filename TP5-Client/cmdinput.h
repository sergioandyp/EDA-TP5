#ifndef CMDINPUT_H
#define CMDINPUT_H

#include "cmdparser.h"
#define PATH_MAX_LEN 150	//Maximum length (in chars) of the file path (including the null terminator) 

void getPath(char*);		//Get path to HTML file

#endif /*CMDINPUT_H*/
